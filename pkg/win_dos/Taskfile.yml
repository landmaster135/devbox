version: "3"

vars:
  DIR_SEP: '{{if eq OS "windows"}}\\{{else}}/{{end}}'
  EXE: '{{if eq OS "windows"}}.exe{{else}}{{end}}'
  CLI_BIN_DIR: "{{.TASKFILE_DIR}}{{.DIR_SEP}}pkg{{.DIR_SEP}}bin{{.DIR_SEP}}cli{{.DIR_SEP}}win_amd64"
  INTERACTIVE_BIN: "{{.CLI_BIN_DIR}}{{.DIR_SEP}}interactive-input.exe"
  INTERACTIVE_TEXT_INPUT: |
    function input_text_interactively() {
      local prompt="$1"; local key="$2";
      local value;
      if ! value="$("{{.INTERACTIVE_BIN}}" \
        --prompt "$prompt" --input-type text --key "$key")"; then
        return 1;
      fi; printf '%s\n' "$value";
    }
  INTERACTIVE_CONFIRM_INPUT: |
    function input_confirm_interactively() {
      local prompt="$1"; local key="$2";
      local value;
      if ! value="$("{{.INTERACTIVE_BIN}}" \
        --prompt "$prompt" --input-type confirm --key "$key")"; then
        return 1;
      fi; printf '%s\n' "$value";
    }
  INTERACTIVE_CHOICE_INPUT: |
    function input_choice_interactively() {
      local prompt="$1"; local key="$2"; shift 2 || return 1;
      local value;
      if ! value="$("{{.INTERACTIVE_BIN}}" \
        --prompt "$prompt" --input-type choice --key "$key" "$@")"; then
        return 1;
      fi; printf '%s\n' "$value";
    }
  INTERACTIVE_CHOICE_FLAG_INPUT: |
    function input_choice_flag_interactively() {
      local prompt="$1"; shift 1 || return 1;
      local value;
      if ! value="$("{{.INTERACTIVE_BIN}}" \
        --prompt "$prompt" --input-type choice-flag "$@")"; then
        return 1;
      fi; printf '%s\n' "$value";
    }
  METHOD_FLAG_GUARD: |
    function ensure_method_flag() {
      local error_message="$1"; shift 1 || true;
      if [ "$#" -eq 0 ]; then
        echo "${error_message}" >&2; exit 1;
      fi; local flag;
      for flag in "$@"; do
        if [ -z "$flag" ]; then
          echo "${error_message}" >&2; exit 1;
        fi
      done
    }

tasks:
  arithmetic-calculator:extract-api-cost:
    desc: "arithmetic-calculatorでAPIコストを抽出"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.METHOD_FLAG_GUARD}}

          FILE_PATH="$(input_text_interactively "Input .txt or .md file path to extract API cost: " file)" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$FILE_PATH"

          echo $'\n[Step 01: START]: --- APIコスト抽出処理を実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}arithmetic-calculator{{.EXE}}" \
            -operation parse-api-cost \
            "$FILE_PATH"
          echo $'\n[Step 01: END]: --- APIコスト抽出処理が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  datetime-calculator:extract-working-time:
    desc: "datetime-calculatorで作業時間を抽出"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.METHOD_FLAG_GUARD}}

          FILE_PATH="$(input_text_interactively "Input .txt or .md file path to extract working time: " file-path)" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$FILE_PATH"

          echo $'\n[Step 01: START]: --- 作業時間抽出処理を実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}datetime-calculator{{.EXE}}" \
            -operation parse-time \
            -output-unit minute \
            "$FILE_PATH"
          echo $'\n[Step 01: END]: --- 作業時間抽出処理が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  datetime-calculator:generate-daily-heading:
    desc: "datetime-calculatorで日次見出しを出力"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.METHOD_FLAG_GUARD}}

          DAY_OFFSET="$(input_text_interactively "Input offset days in int type (negative int applicable): " day-offset)" || exit 1
          
          ensure_method_flag "interactive-input から値を取得できませんでした。" "$DAY_OFFSET"

          echo $'\n[Step 01: START]: --- 日次見出し生成処理を実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}datetime-calculator{{.EXE}}" \
            -operation generate-daily-heading \
            "$DAY_OFFSET"
          echo $'\n[Step 01: END]: --- 日次見出し生成処理が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:habit:
    desc: "image-renamer-for-contentでHB画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of habit (HBxxxxxxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation habit \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:date:
    desc: "image-renamer-for-contentでDA画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of date (DAxxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation date \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:mackerel:
    desc: "image-renamer-for-contentでMA画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of mackerel (MAxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation mackerel \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:anime:
    desc: "image-renamer-for-contentでAN画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of anime (ANxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation anime \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:comic:
    desc: "image-renamer-for-contentでCM画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of comic (CMxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation comic \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:web-clip:
    desc: "image-renamer-for-contentでWC画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of web_clip (WCxxxxxxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation web_clip \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:wine:
    desc: "image-renamer-for-contentでWI画像にリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          START_ARG="$(input_text_interactively "Input start of number for content of wine (WIxxxx_01.webp): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [t]='-time' : " \
            --choice-option "n|-name" --choice-option "t|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-content{{.EXE}}" \
            --src . --operation wine \
            --delimiter "" --suffix "" \
            "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:by-2-digits:
    desc: "image-renamerで2桁連番リネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          PREFIX_ARG="$(input_text_interactively "Input prefix to rename image files (e.g. yyyymmdd_00.jpg): " prefix)" || exit 1
          START_ARG="$(input_text_interactively "Input number to renumber the images  (e.g. prefix_xx.jpg): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [d]='-time' : " \
            --choice-option "n|-name" --choice-option "d|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" \
            "$PREFIX_ARG" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer{{.EXE}}" \
            --src . -digits 2 --delimiter "_" \
            "$PREFIX_ARG" "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:by-4-digits:
    desc: "image-renamerで4桁連番リネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          PREFIX_ARG="$(input_text_interactively "Input prefix to rename image files (e.g. yyyymmdd_0000.jpg): " prefix)" || exit 1
          START_ARG="$(input_text_interactively "Input number to renumber the images  (e.g. prefix_xxxx.jpg): " start)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [d]='-time' : " \
            --choice-option "n|-name" --choice-option "d|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" \
            "$PREFIX_ARG" "$START_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer{{.EXE}}" \
            --src . -digits 4 --delimiter "_" \
            "$PREFIX_ARG" "$START_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  image-renamer:by-prefix:
    desc: "image-renamerで特定のディレクトリ内で接頭辞を使ったリネーム"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_TEXT_INPUT}} {{.INTERACTIVE_CHOICE_FLAG_INPUT}} {{.METHOD_FLAG_GUARD}}

          PREFIX_ARG="$(input_text_interactively "Input prefix of pictures (yyyyMMddHH): " prefix)" || exit 1
          SORT_FLAG="$(input_choice_flag_interactively \
            "Select how to sort image files to rename  [n]='-name'  [d]='-time' : " \
            --choice-option "n|-name" --choice-option "d|-time")" || exit 1

          ensure_method_flag "interactive-input から値を取得できませんでした。" \
            "$PREFIX_ARG" "$SORT_FLAG"

          echo $'\n[Step 01: START]: --- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer{{.EXE}}" \
            -src ".{{.DIR_SEP}}1-3_image_renamer_with_prefix_input" \
            --digits 4 --delimiter "" --start 1 \
            "$PREFIX_ARG" "$SORT_FLAG"
          echo $'\n[Step 01: END]: --- 画像リネームプログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true

  screenshot:rename-convert:
    desc: "スクショの命名＋WebP変換 (image-renamer-for-screenshot + image-converter)"
    cmds:
      - cmd: |
          set -eu
          {{.INTERACTIVE_CHOICE_INPUT}} {{.INTERACTIVE_CONFIRM_INPUT}} {{.METHOD_FLAG_GUARD}}

          METHOD_FLAG="$(input_choice_interactively \
            "Select how to rename image files  [v]='vlc'  [w]='win'  [x]='xiaomi'  [p]='pixel (screen_record)' : " operation \
            --choice-option "v|vlc" --choice-option "w|win" --choice-option "x|xiaomi" --choice-option "p|pixel")" || exit 1

          ensure_method_flag "interactive-input からリネーム方法を取得できませんでした。" "$METHOD_FLAG"

          MOVE_FLAG="$(input_confirm_interactively \
            "Select whether to move original image files to archive directory or not  [y]='--move'  [n]='' : " move)" || exit 1

          echo $'\n[Step 01: START]--- 画像リネームプログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-renamer-for-screenshot{{.EXE}}" -src . "${METHOD_FLAG}"
          echo $'\n[Step 01: END]--- 画像リネームプログラムの実行が完了しました ---'

          echo $'\n[Step 02: START]--- Webp変換プログラムを実行します ---'
          "{{.CLI_BIN_DIR}}{{.DIR_SEP}}image-converter{{.EXE}}" \
            --src . --ext webp \
            --q 80  --archive .{{.DIR_SEP}}5_original_files \
            $MOVE_FLAG
          echo $'\n[Step 02: END]--- Webp変換プログラムの実行が完了しました ---'
        dir: "{{.TASKFILE_DIR}}"
        interactive: true
        silent: true
