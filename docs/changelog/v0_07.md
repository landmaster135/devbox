# Changelog v0.07

## v0.07 — 2026-02-04

PR: #21

### Features
| service | cli | mcp | gRPC | HTTP | 説明 |
| :-- | :--: | :--: | :--: | :--: | :-- |
| cron-workflow | ✅ | ❌️ | ❌️ | ✅ | `cmd/cli/cron-workflow` と `cmd/http` を連携させ、HTTPサーバーとスケジューラを同居。templダッシュボードから手動実行と可視化を操作可能にした。 |
| logging | ❌️ | ❌️ | ❌️ | ❌️ | 標準`log`を廃止し、`StructuredLogger` でHTTP/cron/Dockerビルドのログ形式を統一。Graceful shutdown時のログ粒度も整備。 |
| docker | ✅ | ❌️ | ❌️ | ❌️ | `env-into-compose` / `ports-into-compose` / `volumes-into-compose` / `user-into-compose` を追加し、`env.yml` から `docker-compose.yml` 同期を自動化。 |
| taskfile | ✅ | ❌️ | ❌️ | ❌️ | `inspect` / `fill` / `new` を実装し、テンプレートとの差分検出・補完・新規生成をJSON応答で実行可能にした。 |
| html-sanitizer | ✅ | ❌️ | ❌️ | ❌️ | DOMクレンジングを共通化し、main/article抽出、denyリスト除去、属性除去、空行圧縮をCLIで一括実行可能にした。 |
| yaml-parser | ✅ | ❌️ | ❌️ | ❌️ | `read` / `parse` に加えて `edit-file` を追加し、`--key-value-list` でネスト値を編集しつつキー順・コメント維持に対応。 |
| agent-skills | ❌️ | ❌️ | ❌️ | ❌️ | `supabase-postgres-best-practices` を含む多数のスキルを `.config/codex/skills` に追加し、SKILL/README/参照資料とともにレジストリを拡張。 |

### Improvements
- **postgresql (CLI/MCP)**: ダンプ機能を再設計し、単一テーブルだけでなく全テーブル一括ダンプや並列制御、件数制限、タイムゾーン指定付きファイル名、JSON/Markdownの結果要約、成功/失敗テーブル集計をサポート。writer・meta_fetch分離と追加テストでCLI/MCP双方の再利用性が上がった。
- **ocr-executor-with-ai**: 外部AI選択肢を拡張し、Geminiに加えてVertex AIとOllamaを切り替え可能に。Markdownテーブル出力、ディレクトリ再帰処理、Palworldステータス解析タスクやデフォルトパス指定も追加し、スクリーンショットからの構造化テキスト抽出を自動化。
- **web-scraper**: `get_meta_props`オペレーションでリダイレクト後URLとタイトルだけをJSON返却できるようになり、`interactive-input`経由のTaskfileプリセットでDOM抽出/メタ取得を切り替えながら実行可能に。
- **iso8601-converter**: フラグを`--operation`へ統一し、`now`結果をISO/UNIX/日付(`YYYYMMDD`)のいずれかに絞れる`--format`や、JST扱いの有無を組み合わせた日付→UNIX変換を追加。Taskfileからのテンプレ生成や自動スクリプトが簡潔になった。
- **Taskfile automation**: `pkg/taskfile`配下のUIタスクを全面拡充し、PDF暗号化/復号/抽出/結合、Kana・diff-dreamer連携、映画のGIF/WEBM変換、db-server向けハビットデータ投入、HTTPリクエスト送信、自動シンボル付きお気に入り登録などを一括で実行できるよう整理。
